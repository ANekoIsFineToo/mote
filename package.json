{
  "name": "mote",
  "version": "0.1.0",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cpy _redirects build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "cpy-cli": "^1.0.1",
    "react-scripts": "1.0.7"
  }
}
